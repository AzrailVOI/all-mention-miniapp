# Руководство по внесению вклада

Спасибо за интерес к проекту! Это руководство поможет вам внести вклад в развитие All Mention Bot.

## 🚀 Начало работы

1. **Форкните репозиторий** на GitHub
2. **Клонируйте ваш форк**:
   ```bash
   git clone https://github.com/your-username/all-mention-miniapp.git
   cd all-mention-miniapp
   ```
3. **Создайте ветку** для ваших изменений:
   ```bash
   git checkout -b feature/your-feature-name
   ```

## 📋 Процесс разработки

### 1. Настройка окружения

```bash
# Создайте виртуальное окружение
python -m venv venv

# Активируйте его
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

### 2. Создайте файл `.env`

Скопируйте `.env.example` (если есть) или создайте `.env` с необходимыми переменными:
```env
BOT_TOKEN=your_test_bot_token
WEBAPP_URL=http://localhost:5000
LOG_LEVEL=DEBUG
```

### 3. Стиль кода

- **Python**: Следуйте PEP 8
- **Типизация**: Используйте type hints для всех функций
- **Docstrings**: Добавляйте docstrings в формате Google style
- **Логирование**: Используйте `logger` вместо `print()`

Пример:
```python
def process_chat(chat_id: int) -> Dict[str, Any]:
    """
    Обрабатывает чат и возвращает информацию.
    
    Args:
        chat_id: ID чата для обработки
        
    Returns:
        Словарь с информацией о чате
    """
    logger.info(f"Обработка чата {chat_id}")
    # ...
```

### 4. Тестирование

Перед отправкой PR убедитесь, что:
- Код работает без ошибок
- Нет линтер ошибок
- Логика соответствует требованиям

### 5. Коммиты

Используйте понятные сообщения коммитов:
```bash
git commit -m "feat: добавить фильтрацию чатов по типу"
git commit -m "fix: исправить ошибку валидации WebApp данных"
git commit -m "docs: обновить README с примерами"
```

Префиксы:
- `feat:` - новая функциональность
- `fix:` - исправление ошибок
- `docs:` - документация
- `style:` - форматирование кода
- `refactor:` - рефакторинг
- `test:` - тесты
- `chore:` - обновление зависимостей и т.д.

## 🔍 Структура проекта

### Основные директории

- `bot/` - Код Telegram бота
  - `handlers/` - Обработчики событий (команды, сообщения)
  - `services/` - Бизнес-логика
  - `models/` - Модели данных
  - `utils/` - Утилиты (кэш, ошибки, retry)
  - `config.py` - Конфигурация
  - `constants.py` - Константы

- `webapp/` - Flask веб-приложение
  - `app.py` - Основное приложение
  - `templates/` - HTML шаблоны
  - `static/` - CSS, JavaScript
  - `validators.py` - Валидаторы запросов

### Где что добавлять

- **Новая команда бота**: `bot/handlers/commands.py`
- **Новый обработчик сообщений**: `bot/handlers/messages.py`
- **Новая бизнес-логика**: `bot/services/`
- **Новый API endpoint**: `webapp/app.py`
- **Новая страница**: `webapp/templates/` и соответствующий JS в `webapp/static/js/`

## 🐛 Сообщение об ошибках

При создании issue укажите:
1. **Описание проблемы**: Что произошло?
2. **Шаги для воспроизведения**: Как воспроизвести?
3. **Ожидаемое поведение**: Что должно было произойти?
4. **Фактическое поведение**: Что произошло на самом деле?
5. **Окружение**: Версия Python, ОС, версия бота
6. **Логи**: Соответствующие логи (если есть)

## 💡 Предложения по улучшению

При создании issue с предложением:
1. **Описание**: Что вы хотите добавить/изменить?
2. **Обоснование**: Зачем это нужно?
3. **Примеры использования**: Как это будет использоваться?

## 🔄 Pull Request

1. **Обновите вашу ветку**:
   ```bash
   git checkout main
   git pull upstream main
   git checkout feature/your-feature-name
   git rebase main
   ```

2. **Проверьте код**:
   - Нет линтер ошибок
   - Код работает
   - Документация обновлена

3. **Создайте Pull Request**:
   - Опишите изменения
   - Укажите связанные issues
   - Добавьте скриншоты (если применимо)

4. **Дождитесь ревью**:
   - Отвечайте на комментарии
   - Вносите изменения по запросу
   - Будьте терпеливы

## 📚 Полезные ресурсы

- [Telegram Bot API](https://core.telegram.org/bots/api)
- [python-telegram-bot документация](https://python-telegram-bot.org/)
- [Flask документация](https://flask.palletsprojects.com/)
- [PEP 8](https://pep8.org/)

## ❓ Вопросы?

Если у вас есть вопросы, создайте issue с меткой `question`.

Спасибо за ваш вклад! 🎉
