# Инструкция по настройке Mini App

## Быстрый старт

### 1. Локальная разработка с ngrok

1. Установите ngrok: https://ngrok.com/download

2. Запустите бота:
   ```bash
   python main.py
   ```

3. В другом терминале запустите ngrok:
   ```bash
   ngrok http 5000
   ```

4. Скопируйте HTTPS URL от ngrok (например: `https://abc123.ngrok.io`)

5. Обновите `.env` файл:
   ```
   WEBAPP_URL=https://abc123.ngrok.io
   ```

6. Перезапустите бота

7. Зарегистрируйте Mini App через @BotFather:
   - Откройте @BotFather в Telegram
   - Отправьте `/newapp`
   - Выберите вашего бота
   - Введите название приложения
   - Введите описание
   - Загрузите иконку (опционально)
   - Введите URL: `https://abc123.ngrok.io` (ваш ngrok URL)

8. Откройте приватный чат с ботом и отправьте `/start` или `/chats`

### 2. Продакшен (VPS/Cloud)

1. Разместите код на сервере

2. Настройте веб-сервер (Nginx) для проксирования на Flask:
   ```nginx
   server {
       listen 80;
       server_name your-domain.com;
       
       location / {
           proxy_pass http://127.0.0.1:5000;
           proxy_set_header Host $host;
           proxy_set_header X-Real-IP $remote_addr;
       }
   }
   ```

3. Настройте SSL (Let's Encrypt):
   ```bash
   certbot --nginx -d your-domain.com
   ```

4. Обновите `.env`:
   ```
   WEBAPP_URL=https://your-domain.com
   ```

5. Зарегистрируйте Mini App через @BotFather с URL: `https://your-domain.com`

## Проверка работы

1. Откройте приватный чат с ботом
2. Отправьте `/start` или `/chats`
3. Нажмите на кнопку "📱 Открыть Mini App"
4. Должно открыться веб-приложение со списком чатов

## Устранение проблем

### Mini App не открывается
- Проверьте, что `WEBAPP_URL` в `.env` правильный
- Убедитесь, что веб-сервер запущен (проверьте логи)
- Проверьте, что URL доступен из интернета (для продакшена)

### Список чатов пустой
- Бот автоматически регистрирует чаты при получении сообщений
- Отправьте сообщение боту в группе или приватном чате
- Перезапустите бота и попробуйте снова

### Ошибки в логах
- Проверьте, что все зависимости установлены: `pip install -r requirements.txt`
- Убедитесь, что токен бота правильный в `.env`
- Проверьте права доступа к файлам

