# All Mention Bot - Telegram Bot с Mini App

Telegram бот для упоминания всех участников группы с веб-интерфейсом для управления чатами.

## 🚀 Возможности

- **Упоминание всех участников**: Используйте триггеры `@all`, `@everyone`, `@everybody_mention_bot` для упоминания всех участников группы
- **Mini App**: Веб-интерфейс для просмотра всех чатов, где добавлен бот
- **Управление участниками**: Просмотр списка участников каждого чата с информацией о статусе
- **Фильтрация и сортировка**: Фильтрация чатов по типу и сортировка по различным критериям
- **Кэширование**: Быстрая загрузка данных благодаря кэшированию
- **Graceful degradation**: Показ кэшированных данных при недоступности API

## 📋 Требования

- Python 3.8+
- Telegram Bot Token
- Flask и зависимости (см. `requirements.txt`)

## 🛠️ Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd all-mention-miniapp
```

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Создайте файл `.env` в корне проекта:
```env
BOT_TOKEN=your_telegram_bot_token
WEBAPP_SECRET_KEY=your_webapp_secret_key  # Опционально, по умолчанию используется BOT_TOKEN
WEBAPP_URL=https://your-domain.com  # URL вашего Mini App
WEBAPP_HOST=0.0.0.0
WEBAPP_PORT=5000
LOG_LEVEL=INFO
LOG_JSON=false  # true для JSON формата логирования (продакшен)
```

4. Запустите бота:
```bash
python main.py
```

## 📖 Использование

### Команды бота

- `/start` - Начать работу с ботом
- `/help` - Показать справку по всем командам
- `/chats` - Открыть Mini App со списком чатов (только в приватном чате)
- `/register` - Зарегистрировать текущий чат в Mini App (только в группах)
- `/stats` - Показать статистику по зарегистрированным чатам

### Триггеры упоминания

В группах используйте следующие триггеры для упоминания всех участников:
- `@all`
- `@everybody_mention_bot`
- `@everyone`

Бот автоматически:
- Упомянет всех участников (кроме ботов)
- Покажет автора сообщения
- Удалит оригинальное сообщение

### Mini App

1. Откройте бота в приватном чате
2. Используйте команду `/chats` или кнопку в `/help`
3. Просматривайте список всех чатов, где добавлен бот
4. Фильтруйте и сортируйте чаты по различным критериям
5. Просматривайте участников каждого чата

## 🏗️ Архитектура

```
all-mention-miniapp/
├── bot/                    # Код бота
│   ├── config.py          # Конфигурация
│   ├── constants.py       # Константы
│   ├── handlers/          # Обработчики событий
│   │   ├── commands.py    # Команды бота
│   │   ├── messages.py   # Обработка сообщений
│   │   └── chat_events.py # События чата
│   ├── models/            # Модели данных
│   ├── services/          # Бизнес-логика
│   │   ├── chat_service.py
│   │   ├── mention_service.py
│   │   └── chat_storage_service.py
│   └── utils/             # Утилиты
│       ├── cache.py       # Кэширование
│       ├── errors.py      # Обработка ошибок
│       ├── retry.py       # Retry логика
│       └── webapp_validator.py # Валидация WebApp
├── webapp/                # Веб-приложение (Mini App)
│   ├── app.py            # Flask приложение
│   ├── templates/        # HTML шаблоны
│   ├── static/           # Статические файлы
│   │   ├── css/          # Стили
│   │   └── js/           # JavaScript
│   └── validators.py     # Валидаторы запросов
├── main.py               # Точка входа
└── requirements.txt      # Зависимости
```

## 🔒 Безопасность

- **Валидация WebApp данных**: Полная проверка подписи через HMAC-SHA256
- **Rate Limiting**: Ограничение количества запросов к API
- **CORS**: Настройка CORS для безопасности
- **Валидация входных данных**: Проверка всех пользовательских данных через Pydantic

## 📊 Мониторинг

- **Health Check**: `/health` - проверка состояния сервиса
- **Метрики**: `/api/metrics` - метрики производительности API
- **Логирование**: Структурированное логирование (JSON формат для продакшена)

## 🧪 Разработка

### Структура проекта

Проект следует принципам чистой архитектуры:
- Разделение на слои (handlers, services, models)
- Dependency injection через глобальные экземпляры
- Централизованная обработка ошибок
- Кэширование для производительности

### Добавление новых функций

1. Создайте обработчик в `bot/handlers/`
2. Добавьте бизнес-логику в `bot/services/`
3. Зарегистрируйте обработчик в `main.py`
4. Обновите документацию

## 📝 Лицензия

[Укажите лицензию]

## 🤝 Вклад

См. [CONTRIBUTING.md](CONTRIBUTING.md) для руководства по внесению вклада.

## 📞 Поддержка

[Укажите контакты для поддержки]
