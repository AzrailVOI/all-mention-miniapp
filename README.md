# All Mention Bot

Telegram бот для упоминания всех участников группы по триггеру `@all`.

## Возможности

- ✅ Упоминание всех участников группы (кроме ботов) по триггерам: `@all`, `@everybody_mention_bot`, `@everyone`
- ✅ Автоматическое удаление оригинального сообщения
- ✅ Отображение автора сообщения
- ✅ **Mini App для просмотра всех чатов, где добавлен бот** (включая приватные)
- ✅ Обработка ошибок и логирование
- ✅ Чистая архитектура с разделением на слои

## Установка

1. Клонируйте репозиторий или скачайте файлы

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Создайте файл `.env` на основе `.env.example`:
```bash
cp .env.example .env
```

4. Добавьте токен бота в `.env`:
```
BOT_TOKEN=your_bot_token_here
LOG_LEVEL=INFO
WEBAPP_URL=http://localhost:5000
WEBAPP_HOST=0.0.0.0
WEBAPP_PORT=5000
```

## Запуск

```bash
python main.py
```

## Настройка бота в группе

1. Добавьте бота в группу
2. Сделайте бота администратором
3. Дайте боту права на удаление сообщений
4. Используйте триггеры `@all`, `@everybody_mention_bot` или `@everyone` в сообщениях

## Настройка Mini App

1. **Для локальной разработки:**
   - Убедитесь, что `WEBAPP_URL` в `.env` указывает на ваш локальный адрес
   - Используйте ngrok или аналогичный сервис для туннелирования:
     ```bash
     ngrok http 5000
     ```
   - Обновите `WEBAPP_URL` в `.env` на URL от ngrok

2. **Для продакшена:**
   - Разместите веб-приложение на хостинге (Heroku, Railway, VPS и т.д.)
   - Обновите `WEBAPP_URL` в `.env` на публичный URL
   - Зарегистрируйте Mini App через @BotFather:
     ```
     /newapp
     Выберите бота
     Введите название приложения
     Введите описание
     Загрузите иконку (опционально)
     Введите URL веб-приложения
     ```

3. **Использование:**
   - Откройте приватный чат с ботом
   - Отправьте команду `/start` или `/chats`
   - Нажмите на кнопку "📱 Открыть Mini App"
   - В Mini App вы увидите список всех чатов, где добавлен бот

## Структура проекта

```
all-mention-bot/
├── bot/
│   ├── __init__.py
│   ├── config.py                    # Конфигурация
│   ├── handlers/                    # Обработчики
│   │   ├── commands.py              # Команды (/start, /chats)
│   │   ├── messages.py              # Обработка сообщений
│   │   └── chat_events.py           # События чата
│   ├── services/                    # Бизнес-логика
│   │   ├── chat_service.py          # Работа с чатами
│   │   ├── mention_service.py       # Упоминания
│   │   └── chat_storage_service.py  # Хранение информации о чатах
│   └── models/                      # Модели данных
│       └── message.py
├── webapp/                          # Mini App
│   ├── __init__.py
│   ├── app.py                       # Flask приложение
│   ├── templates/
│   │   └── index.html               # Главная страница
│   └── static/
│       ├── css/
│       │   └── style.css            # Стили
│       └── js/
│           └── app.js               # JavaScript логика
├── main.py                          # Точка входа
├── requirements.txt
├── .env.example
└── README.md
```

## Примечания

- Бот работает только в группах и супергруппах
- Требуются права администратора
- Telegram API ограничивает доступ к полному списку участников, поэтому используются доступные методы получения участников
- Mini App автоматически регистрирует все чаты, где бот получает сообщения или добавляется
- Для работы Mini App требуется публичный URL (используйте ngrok для разработки)

