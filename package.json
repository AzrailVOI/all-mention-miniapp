{
  "name": "all-mention-miniapp",
  "version": "1.0.0",
  "description": "Telegram Mini App for All Mention Bot",
  "scripts": {
    "build:ts": "tsc --project tsconfig.json",
    "watch:ts": "tsc --watch --project tsconfig.json",
    "build": "npm run build:ts",
    "start": "python main.py",
    "dev": "npm run build:ts && python main.py",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
